import styled from 'styled-components';
import PseudoElement from './PseudoElement';


const Heading = styled(PseudoElement)`
  font-size: 1.5rem;
  font-family: 'Roboto, Bold';
`

export default Heading;